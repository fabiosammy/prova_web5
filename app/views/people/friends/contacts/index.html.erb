<p id="notice"><%= notice %></p>

<h1>Listing Contacts of <%= @friend.name %></h1>

detalhe: <%= @person.name %>
<table>
  <thead>
    <tr>
      <th>Type</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.type %></td>
        <td><%= contact.value %></td>
        <td><%= link_to 'Show', person_friend_contact_path(@person, @friend, contact) %></td>
        <td><%= link_to 'Edit', edit_person_friend_contact_path(@person,@friend, contact) %></td>
        <td><%= link_to 'Destroy', person_friend_contact_path(@person, @friend, contact), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contact', new_person_friend_contact_path(@person, @friend) %>

